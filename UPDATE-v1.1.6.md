# v61 / Android v1.1.6 변경 사항

- 로그인 화면의 '로그인 유지 (30일)'를 체크하고 한 번 로그인하면 앱 종료 및 서버 재시작 후에도 유지됩니다. 비밀번호 대신 해시 처리한 세션 식별자를 data/sessions.json에 저장합니다. 로그아웃, 비밀번호 변경, 30일 만료 시 해제됩니다. 유지 옵션을 끄면 기존 30분 미사용 / 최대 12시간 제한을 사용합니다.
- 내부 IP 주소와 외부 도메인은 서로 다른 로그인 주소입니다. 각각 최초 1회 로그인해야 합니다.
- 완료·실패·중단된 작업에서도 확인된 파일을 다시 선택할 수 있습니다. 실행/대기열에 있는 작업은 중단 또는 완료 후 선택하세요. 기존 완료 파일을 건너뛰는 동작은 유지됩니다.
- 파일 및 폴더 체크박스, 하위 폴더 포함 선택, 선택 용량 합계를 지원합니다. 검색 중 폴더 선택은 검색 결과에 포함된 파일에 적용됩니다.
- 원본 메타데이터와 저장된 파일 크기를 표시합니다. ApexDrive 화면의 반올림된 용량은 '약'으로 표시합니다. 이전 작업에도 저장 파일 크기를 보완합니다.
- '용량 다시 확인'은 현재 페이지의 크기 없는 파일을 최대 20개씩 원본 HTTP 헤더로 조회합니다. 원본 주소가 없거나 인증/만료/스트리밍 등으로 크기를 제공하지 않으면 '원본 용량 정보 없음'을 표시합니다. 정확한 크기를 알아낼 수 없는 파일을 0바이트로 표시하지 않습니다.
- 작업/파일 하단 관리막대를 화면 아래와 좌우 끝에 고정합니다. 화면 크기에 맞게 조정하며 시스템 안전 영역은 내부 여백으로 처리합니다.
- Android 시스템 영역과 WebView의 중복 여백을 방지하고 시스템 영역 배경을 어둡게 맞췄습니다. 상태 표시줄/내비게이션 버튼 자체의 필수 영역과 연결 설정 도구막대는 유지됩니다.

## 서버 적용
압축 안 auto-video-downloader 폴더의 소스를 기존 /volume5/22TB/docker/auto 에 덮어쓰세요. 기존 .env, data, downloads 폴더를 삭제하지 마세요.

```sh
cd /volume5/22TB/docker/auto && docker compose down && docker compose up -d --build
```

서버 업데이트 후 새 로그인 유지 옵션으로 다시 로그인하세요.

## 앱 적용
Android Studio에서 Android 소스 폴더를 열고 Gradle 동기화 후 Generate APKs / assembleDebug로 빌드합니다. 결과: app/build/outputs/apk/debug/AVDv1.1.6.apk. 기존 앱과 같은 서명으로 업데이트 설치해야 앱 설정과 쿠키가 보존됩니다.

## 검증 범위
Python 문법 검사, JavaScript 문법 검사, 인증/파일 선택/용량 관련 32개 테스트 통과. 수정된 파일 선택 테스트는 폴더 행과 여러 줄 함수 형식에 맞춰 갱신했습니다.
Android SDK/Gradle이 제공되지 않는 환경이라 APK 빌드 및 실제 Android 기기/UI 렌더 검증은 수행하지 못했습니다. 외부 사이트의 실제 용량 응답도 이 환경에서 검증하지 못했습니다.
