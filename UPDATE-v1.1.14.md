# Android 1.1.14

- `versionCode`를 15로 올려 동일한 서명 키로 빌드한 APK가 기존 앱 위에 업데이트되도록 했습니다.
- 연결 설정 화면의 상단을 `←`와 `연결 설정` 제목만 표시하는 앱 바로 정리했습니다.
- `현재 네트워크 사용`과 `엔드포인트 추가`는 보조 Outlined 버튼으로 변경했습니다.
- `저장하고 연결`은 강조 버튼으로 유지하고 화면 하단에 고정했습니다.
- 설정 입력 영역은 키보드가 표시될 때 크기가 조정되며 별도로 스크롤됩니다.
